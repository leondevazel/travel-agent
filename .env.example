ANTHROPIC_API_KEY=sk-ant-...
# 1 = skip every agent and serve a canned sample trip: instant, no API cost.
# Use it for any UI/animation work. Leave unset for real trips.
MOCK_MODE=0
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/travel_agent
METRICS_LOG_PATH=logs/metrics.jsonl
